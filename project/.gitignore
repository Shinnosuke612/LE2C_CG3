# Generated skybox backups can exceed GitHub's 100 MB file limit.
resources/skyboxes/*.dds.bak

# Scene saves use these only while replacing or recovering the primary file.
resources/scenes/*.json.bak
resources/scenes/*.json.tmp
