# {{DISPLAY_NAME}}

This is a Phase 0 Empty Game Project generated from the current engine checkout.

- Project ID: `{{PROJECT_ID}}`
- Visual Studio solution: `{{SOLUTION_PATH}}`
- Resource root: `project/resources`
- Startup Scene: `{{START_SCENE_ID}}`

`Debug` and `Development` start the Editor profile. `Release` starts the Runtime profile.

The generated source is an independent snapshot. Later Template or Engine changes are not applied automatically.

Git initialization, remote setup, Push, and Visual Studio launch are intentionally left to an explicit user action.
